apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: core
  namespace: wholesky
spec:
  template:
    spec:
      containers:
        - name: skyd
          resources:
            requests: {cpu: 500m, memory: 1Gi}
            limits: {memory: 2Gi}
