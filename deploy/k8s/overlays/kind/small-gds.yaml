apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gds
  namespace: wholesky
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: skyd
          resources:
            requests: {cpu: 250m, memory: 1Gi}
            limits: {memory: 2Gi}
